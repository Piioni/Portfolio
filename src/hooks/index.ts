export { useTheme } from './useTheme';

// Note: useMediaQuery is available but rarely needed when using Tailwind's
// responsive classes (sm:, md:, lg:, etc.) for styling.
// Only import it directly if you need JS-based responsive logic:
// import { useMediaQuery } from '@/hooks/useMediaQuery';
